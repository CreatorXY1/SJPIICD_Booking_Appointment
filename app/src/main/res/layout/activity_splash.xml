<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/splash_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/splash_gradient">

    <!-- Optional logo (replace with your drawable or remove) -->
    <ImageView
        android:id="@+id/ivSplashLogo"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_gravity="center_horizontal|center_vertical"
        android:src="@mipmap/ic_sjpiicdaa"
        android:contentDescription="App logo"
        android:scaleType="centerInside"
        android:layout_marginBottom="48dp"
        android:elevation="8dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_gravity="center_horizontal|bottom"
        android:gravity="center"
        android:layout_marginBottom="96dp">

        <TextView
            android:id="@+id/tvAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SAA — SJPIICD"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:shadowColor="#00000066"
            android:shadowRadius="3"
            android:shadowDx="0"
            android:shadowDy="2"/>

        <TextView
            android:id="@+id/tvTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Student Appointment App"
            android:textSize="14sp"
            android:textColor="#E1BEE7"
            android:layout_marginTop="8dp"/>
    </LinearLayout>

    <!-- small indeterminate progress at bottom to hint 'loading' -->
    <ProgressBar
        android:id="@+id/pbSplash"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="32dp"
        android:indeterminate="true"
        android:indeterminateTint="#BA68C8"
        android:visibility="visible"/>
</FrameLayout>